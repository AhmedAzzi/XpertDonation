<Window x:Class="XDonation.Views.DrugEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource ModernDialogStyle}">

    <Border Background="White" CornerRadius="12" Margin="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.18" ShadowDepth="4" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Style="{StaticResource DialogHeaderStyle}">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="💊" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title}" Foreground="White" FontSize="18" FontFamily="Segoe UI Semibold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Click="Close_Click" Style="{StaticResource CloseButtonStyle}" ToolTip="Fermer"/>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="30,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Désignation *" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,16">
                    <TextBlock Text="DCI (Dénomination Commune Internationale)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Dci, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Margin="0,0,0,16">
                    <TextBlock Text="Forme (ex: Gélule, Sirop...)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Form, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="6" Margin="0,0,0,16">
                    <TextBlock Text="Code-barres Interne (Généré)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource BarcodeInputStyle}" IsEnabled="False" Opacity="0.7" ToolTip="Généré automatiquement si laissé vide"/>
                </StackPanel>

                <StackPanel Grid.Row="7" Margin="0,0,0,16">
                    <TextBlock Text="Code-barres Fabricant" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding CodeBarresFabricant, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource BarcodeInputStyle}"/>
                </StackPanel>

                <!-- Status Message -->
                <Border Grid.Row="8" 
                        Background="#FEF2F2" 
                        BorderBrush="#FEE2E2" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="12,8"
                        Visibility="{Binding IsStatusError, Converter={StaticResource BoolVis}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,12,12" Padding="24,18" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock Text="* Champs obligatoires" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Annuler" Command="{Binding CancelCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource GhostButtonStyle}" Width="100" Margin="0,0,12,0"/>
                        <Button Content="Enregistrer" Command="{Binding SaveCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource PrimaryButtonStyle}" MinWidth="130"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>

